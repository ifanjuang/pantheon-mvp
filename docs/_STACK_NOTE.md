# Stack note

Status: support note.

This branch is stacked after `agent/document-runtime-status` / pantheon-mvp#61. Merge/rebase ordering must preserve that dependency until the stack is consolidated.
