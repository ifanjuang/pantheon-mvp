# Closed decision vocabulary (Option A2). The gate reads its allowed decisions
# from here — never from the candidate stream — so decision semantics cannot be
# driven by input.
#
# RECONCILED with upstream: the vendored schema (UPSTREAM_COMMIT dc9068e) now
# defines a `decision_value` enum, and it is EXACTLY these four values. This file
# is therefore no longer a guess — it matches the canonical upstream vocabulary.
# It is kept as the gate's authority (a single small file to read) and stays in
# sync with schema `$defs.decision_value`. If the two ever diverge, the schema
# is authoritative and this file must be re-synced.
status: matches_upstream_decision_value  # @ schema dc9068e
allowed_decisions:
  - approve
  - refuse
  - request_revision
  - request_more_evidence
